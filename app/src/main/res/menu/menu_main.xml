<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/action_mode_close_button"
          android:title="Salir"
          android:orderInCategory="100"
          app:showAsAction="never"/>
      <item
          android:id="@+id/action_search_automovil"
          android:title="Search"
          android:icon="@drawable/ico_search"
          app:showAsAction="collapseActionView|ifRoom"
          android:actionViewClass="android.widget.SearchView"/>

    <item android:id="@+id/action_add_automovil"
          android:title="@string/automovil_info_add_automovil"
          android:orderInCategory="100"
          app:showAsAction="ifRoom|withText"
          android:icon="@drawable/ic_action_new_automovil"/>

</menu>
